apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.resultApp.name }}-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      name: {{ .Values.resultApp.name }}-pod
      app: {{ .Values.application.name }}
  template:
    metadata: 
      name: {{ .Values.resultApp.name }}
      labels:
        name: {{ .Values.resultApp.name }}-pod
        app: {{ .Values.application.name }}
    spec: 
      containers:
        - name: {{ .Values.resultApp.name }}
          image: {{ .Values.resultApp.image }}
          ports:
            - containerPort: {{ .Values.resultApp.containerPort }}

---
apiVersion: v1
kind: Service
metadata: 
  name: {{ .Values.resultApp.name }}-service
  labels:
    name: {{ .Values.resultApp.name }}-service
    app: {{ .Values.application.name }}
spec: 
  type: {{ .Values.resultApp.porttype }}
  selector: 
    name: {{ .Values.resultApp.name }}-pod
    app: {{ .Values.application.name }}
  ports:
    - port: {{ .Values.resultApp.containerPort }}
      targetPort: {{ .Values.resultApp.containerPort }}
      nodePort: {{ .Values.resultApp.nodePort }}